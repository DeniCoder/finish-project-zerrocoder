def format_rub(amount: float) -> str:
    """
    Форматирование числа как суммы в рублях: 42 850,00 (десятичная запятая, неразрывный пробел).
    """
    # Используем неразрывный ("узкий") пробел для единообразия в UI
    s = f"{amount:,.2f}".replace(",", " ").replace(".", ",")
    return s
